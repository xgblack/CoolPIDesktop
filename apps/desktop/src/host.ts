import {invoke} from '@tauri-apps/api/core';import type {HostError,TaskSnapshot,RuntimeInfo,ObserverInfo} from '../../../packages/host-contract/src';
export const host={runtime:(explicit:string)=>invoke<RuntimeInfo>('runtime_status',{explicit:explicit.trim()||null}),start:(taskId:string,explicit:string)=>invoke<TaskSnapshot>('start_task',{taskId,explicit:explicit.trim()||null}),list:()=>invoke<TaskSnapshot[]>('list_tasks'),snapshot:(taskId:string)=>invoke<TaskSnapshot>('task_snapshot',{taskId}),prompt:(taskId:string,message:string)=>invoke<TaskSnapshot>('prompt_task',{taskId,message}),abort:(taskId:string)=>invoke<TaskSnapshot>('abort_task',{taskId}),stop:(taskId:string)=>invoke<TaskSnapshot>('stop_task',{taskId}),restart:(taskId:string)=>invoke<TaskSnapshot>('restart_task',{taskId}),respond:(taskId:string,requestId:string,value:string|null,confirmed:boolean|null,cancelled:boolean)=>invoke<TaskSnapshot>('respond_ui',{taskId,requestId,value,confirmed,cancelled}),observer:()=>invoke<ObserverInfo>('observer_info')};
export function hostError(e:unknown):HostError{if(typeof e==='object'&&e&&'message'in e){const v=e as any;return{code:v.code||'host_error',message:String(v.message),suggestion:v.suggestion}}return{code:'host_unavailable',message:String(e||'无法连接桌面 Host'),suggestion:'请检查 Host 进程。'}}
