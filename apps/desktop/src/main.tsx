import { useState } from 'react';
import { invoke } from '@tauri-apps/api/core'; import './style.css';
type Runtime={status:string;executable?:string;version?:string;protocol?:number;error?:string};
export function App(){const [r,setR]=useState<Runtime>({status:'未检测'});const [b,setB]=useState(false);async function detect(){setB(true);try{setR(await invoke<Runtime>('detect_omp',{explicit:null}))}catch(e){setR({status:'检测失败',error:String(e)})}finally{setB(false)}}return <main><h1>OMP Desktop · P0</h1><section><h2>系统 OMP Runtime</h2><p className="status">{r.status}</p><dl><dt>路径</dt><dd>{r.executable??'—'}</dd><dt>版本</dt><dd>{r.version??'—'}</dd><dt>RPC 协议</dt><dd>{r.protocol??'—'}</dd></dl>{r.error&&<pre>{r.error}</pre>}<button onClick={detect} disabled={b}>{b?'检测中…':'检测 OMP'}</button><p className="hint">检测通过 Tauri Rust Host 执行，不下载或替代系统 OMP。</p></section></main>}
