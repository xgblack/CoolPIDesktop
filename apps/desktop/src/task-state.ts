import type {HostEvent,TaskSnapshot} from '../../../packages/host-contract/src';export type TaskMap=Record<string,TaskSnapshot>;export function applySnapshot(t:TaskMap,s:TaskSnapshot){const c=t[s.taskId];if(c&&c.runId===s.runId&&s.seq<c.seq)return t;return {...t,[s.taskId]:{...s,events:s.events.filter(e=>e.taskId===s.taskId&&e.runId===s.runId&&e.seq<=s.seq).sort((a,b)=>a.seq-b.seq).slice(-512)}}}function rec(v:unknown):Record<string,any>{return typeof v==='object'&&v&&!Array.isArray(v)?v as any:{}}export function eventText(e:HostEvent){const p=rec(e.payload),a=rec(p.assistantMessageEvent);return a.type==='text_delta'?a.delta:(e.eventType==='text_delta'||p.type==='text_delta')?p.delta:null}export function pendingRequests(es:HostEvent[]){return es.filter(e=>e.eventType==='extension_ui_request')}
